<%= content_tag :ul do %>
  <% @posts.each do |post| %>
    <% concat(content_tag(:li, link_to(post.title, {:controller => 'post', :action => 'edit', :title => post.title}) )) %>
  <% end %>
<% end %>